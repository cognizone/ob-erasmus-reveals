logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

env:
  name: aws-local
  reveals:
    elastic:
      scheme: http
      host: ob-erasmus-reveals-dev-elastic.cz-aws.net
      jsonApiPort: 9200
      index: aws-dev-reveals-data
    mail:
      fromAddress: 'Reveals'
      fromDescription: ''
      fromEmail: reveals.project.cz@gmail.com
      password: xgpnlbopbaevzmiz
      #SMTP host
      smtp:
        host: smtp.gmail.com
        port: 465
        auth: true
        starttls:
          enable: true
        socketFactory:
          clazz: javax.net.ssl.SSLSocketFactory